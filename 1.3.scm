;; 1.3 define a procedure that takes three number as arguments and returns the sum of the squares of the two larger numbers


(define (larger-two-squared x y z)
  ;; if x is smaller than y and z then return * z z + * y y
  (if (< x y)
     ())

  )

(larger-two-squared 3 5 1)
